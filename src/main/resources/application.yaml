spring:
    application:
        name: rodizioDeVagas
    jackson:
        time-zone: America/Sao_Paulo
    thymeleaf:
        cache: true

app:
    base-url: ${APP_BASE_URL}
    redis:
        stream:
            fiscal-events: fiscal-events
        consumer:
            group: queue-service
            instance: instance-1
    events:
        processing:
            batch-size: 10
            timeout: 3000




